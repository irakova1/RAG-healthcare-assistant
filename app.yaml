runtime: custom
service: init-llm
env: flex
automatic_scaling:
  min_num_instances: 1
  max_num_instances: 1
resources:
   cpu: 4
   memory_gb: 16
   disk_size_gb: 25
network:
  name: healthworks-vpc
  subnetwork_name: healthworks-vpc
